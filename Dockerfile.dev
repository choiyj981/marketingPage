FROM node:18-alpine

WORKDIR /app

# 패키지 파일 복사
COPY package*.json ./

# 의존성 설치
RUN npm ci

# 소스 코드는 볼륨 마운트로 사용하므로 여기서는 복사하지 않음
# 개발 모드에서는 소스 코드 변경이 즉시 반영됩니다

# 포트 노출
EXPOSE 5000

# 환경 변수 설정
ENV NODE_ENV=development
ENV PORT=5000

# 개발 서버 시작 (핫 리로드 지원)
CMD ["npm", "run", "dev"]

